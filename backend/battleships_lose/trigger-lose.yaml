---
apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: trigger-lose
spec:
  broker: default
  filter:
    attributes:
      type: lose
  subscriber:
    ref:
     apiVersion: serving.knative.dev/v1
     kind: Service
     name: battleships--lose

